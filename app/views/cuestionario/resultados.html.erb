<%
  # Helper methods for styling
  def get_category_color_class(score, max_score)
    percentage = (score.to_f / max_score * 100).round
    if percentage >= 60
      "border-coral hover:border-coral"
    elsif percentage >= 40
      "border-yellow hover:border-yellow-600" 
    else
      "border-green-light hover:border-green"
    end
  end

  def get_badge_color_class(score, max_score)
    percentage = (score.to_f / max_score * 100).round
    if percentage >= 60
      "bg-coral text-white"
    elsif percentage >= 40
      "bg-yellow-400 text-gray-800"
    else
      "bg-green-light text-white"
    end
  end

  def get_progress_bar_color(score, max_score)
    percentage = (score.to_f / max_score * 100).round
    if percentage >= 60
      "progress-bar high"
    elsif percentage >= 40
      "progress-bar moderate"
    else
      "progress-bar good"
    end
  end

  def get_recommendation_text(score, max_score)
    percentage = (score.to_f / max_score * 100).round
    if percentage >= 60
      "Este tipo de cansancio está afectando seriamente tu bienestar. Te recomendamos priorizar los descansos específicos para esta área."
    elsif percentage >= 40
      "Es probable que necesites revisar y mejorar tu descanso en esta área."
    else
      "Estás manejando bien tu descanso en esta área."
    end
  end

  # Calculate total affirmative responses
  total_responses = @category_scores.values.sum
  max_possible = @categories.count * 6 * 3 # 7 categories * 6 questions * max score 3
%>

<div class="bg-gradient-cream min-h-screen text-dark leading-relaxed">
  <div class="max-w-5xl mx-auto p-4 md:p-8 bg-warm-white shadow-2xl rounded-3xl my-4 md:my-8 relative overflow-hidden results-container">
    <div class="relative z-10">
      <!-- Header -->
      <div class="text-center mb-12 pb-8 border-b-2 border-green relative header">
        <h1 class="text-5xl md:text-7xl font-bold text-green mb-2 drop-shadow-sm">Tus Resultados</h1>
        <h2 class="text-2xl md:text-3xl text-coral font-normal italic">Cuestionario de Descanso Consciente</h2>
      </div>

      <% if @predominant_tiredness %>
        <!-- Primary Result -->
        <div class="bg-gradient-coral p-8 md:p-12 rounded-3xl mb-12 text-white text-center shadow-xl relative primary-result">
          <div class="bg-white bg-opacity-20 border-4 border-white rounded-2xl px-6 py-4 mx-auto mb-8 text-white shadow-lg max-w-sm">
            <div class="flex items-center justify-center mb-2">
              <span class="text-3xl mr-3">🌸</span>
              <div class="text-center">
                <p class="text-lg font-bold"><%= total_responses %> respuestas afirmativas</p>
                <p class="text-sm opacity-90">de cansancio detectadas</p>
              </div>
            </div>
          </div>
          <h3 class="text-xl md:text-2xl mb-4 opacity-95">Tu tipo de cansancio predominante:</h3>
          <h4 class="text-2xl md:text-4xl mb-6 font-bold drop-shadow-sm"><%= @predominant_tiredness.name %></h4>
          <p class="text-base md:text-lg max-w-2xl mx-auto leading-relaxed opacity-95">
            <%= get_recommendation_text(@category_scores[@predominant_tiredness.id], 18) %>
          </p>
        </div>
      <% end %>

      <!-- Detailed Results -->
      <div class="mb-12">
        <h3 class="text-3xl md:text-5xl text-center text-dark mb-8 font-bold">Resultados Detallados</h3>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mb-8">
          <% @categories.order(:position).each do |category| %>
            <% score = @category_scores[category.id] %>
            <% max_score = 18 %>
            <% percentage = (score.to_f / max_score * 100).round %>
            
            <div class="bg-warm-white border-2 <%= get_category_color_class(score, max_score) %> rounded-3xl p-6 md:p-8 shadow-md hover:-translate-y-1 hover:shadow-xl transition-all duration-300">
              <div class="flex justify-between items-center mb-6">
                <h4 class="text-lg md:text-xl font-bold text-dark"><%= category.name %></h4>
                <div class="px-4 py-2 rounded-3xl font-bold text-lg <%= get_badge_color_class(score, max_score) %>">
                  <span style="font-size: 1.2em"><%= score %></span>/<%= max_score %>
                </div>
              </div>

              <div class="bg-cream h-3 rounded-full overflow-hidden mb-6">
                <div class="<%= get_progress_bar_color(score, max_score) %> h-full rounded-full transition-all duration-500" style="width: <%= percentage %>%"></div>
              </div>
              
              <p class="text-medium leading-relaxed text-base md:text-lg">
                <%= get_recommendation_text(score, max_score) %>
              </p>
            </div>
          <% end %>
        </div>
      </div>

      <!-- Important Note -->
      <div class="bg-gradient-blue border-2 border-blue-500 rounded-3xl p-6 md:p-8 mb-8 relative important-note">
        <h4 class="text-blue-800 text-xl md:text-2xl mb-4 font-bold">Nota importante</h4>
        <p class="text-blue-900 leading-relaxed text-base md:text-lg">
          Si el cansancio afecta profundamente tu calidad de vida y bienestar, 
          o si sentís que no lográs recuperarte a pesar de los descansos, 
          te recomendamos que busques el apoyo de un profesional de la salud. 
          Este cuestionario no reemplaza el consejo o tratamiento profesional.
        </p>
      </div>

      <!-- Creator Info -->
      <div class="text-center">
        <div class="bg-gradient-green p-6 md:p-8 rounded-3xl text-white text-center creator-info">
          <p class="mb-2 text-lg"><strong>Creado por:</strong></p>
          <p class="text-xl font-bold">Gimena Galarraga</p>
          <p class="italic opacity-90 mb-2">Psicóloga especializada en mindfulness, compasión y creatividad</p>
          <p class="text-sm opacity-80">
            Basado en los conceptos de la Dr. Saundra Dalton-Smith sobre los 7 tipos de descanso
          </p>
        </div>
      </div>

      <!-- Back Link -->
      <div class="text-center mt-8">
        <%= link_to "/cuestionario", class: "inline-block px-8 py-3 bg-gray-600 hover:bg-gray-700 text-white rounded-xl font-medium transition-colors duration-200 shadow-lg" do %>
          ← Volver al inicio
        <% end %>
      </div>
    </div>
  </div>
</div>